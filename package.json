{
  "name": "edge-tg-bot-forward",
  "version": "1.0.0",
  "description": "Cloudflare Workers edge proxy for Telegram Bot API with token allowlist",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.41",
    "@cloudflare/workers-types": "^4.20240529.0",
    "typescript": "^5.4.5",
    "vitest": "^2.1.0",
    "wrangler": "^4.95.0"
  }
}
